# tappo-privacy (redirect stub)

The Tappo privacy policy moved to the **TappoLabs** organisation:
<https://tappolabs.github.io/tappo-privacy/> (repo `TappoLabs/tappo-privacy`).

This repo exists only to keep the old URL alive, because GitHub Pages
does **not** redirect after a repository transfer and the old address is
referenced by published Google Play listings.

**Do not delete this repo** and do not disable its Pages site.
